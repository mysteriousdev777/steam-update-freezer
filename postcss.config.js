// PostCSS config picked up automatically by postcss-loader.
// Tailwind v4 ships its PostCSS integration as a separate plugin.
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
