export const GITHUB_URL = 'https://github.com/mysteriousdev777/steam-update-freezer';

// `HEAD` resolves to the repo's default branch on GitHub, so the link survives a branch rename or a
// different release branch — no hardcoded master/main to go stale.
export const PRIVACY_URL = `${GITHUB_URL}/blob/HEAD/PRIVACY.md`;
